{
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/:path*" },
    { "source": "/((?!api/).*)", "destination": "/$1" }
  ],
  "functions": {
    "api/crawl.py": {
      "maxDuration": 60
    }
  }
}